version: '3.8'

services:
  waf-manager:
    build: .
    container_name: hospital-waf-manager
    ports:
      - "8083:8083"
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
    restart: unless-stopped
